public class TripUtils {
  
  protected static double getDistance(Location source, Location destination) {
    return 4.2;
  }

  protected static int getETA(Location user, Location driver) {
    // some method which will constantly get ETA
    return 5;
  }
}
